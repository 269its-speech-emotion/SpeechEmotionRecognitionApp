import colors from "./colors"

const palette = {
    heading:{
        color: colors.title,
        fontsize: 24,
        textAlign: 'center'
    },
    text:{
        color:colors.text, 
        fontsize: 15, 
        textAlign: 'center'
    }

}

export default palette